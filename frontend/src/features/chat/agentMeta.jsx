import { Calendar, Edit3, Github, Mail, UserPlus } from '../../components/common/Icons';

export const CHAT_AGENT_ICONS = {
  mail_agent: Mail,
  contact_agent: UserPlus,
  calendar_agent: Calendar,
  memos_agent: Edit3,
  github_agent: Github,
};

export const CHAT_AGENT_IDS = Object.keys(CHAT_AGENT_ICONS);
