# friday

- `backend/` — FastAPI + LangChain
- `frontend/` — React (Vite)
- `supabase/` — Supabase project config (linked to `sbqgivaqomoyobfamwxt`)

## Dev

```bash
# backend
cd backend && source .venv/bin/activate && uvicorn app.main:app --reload --port 8005

# frontend
cd frontend && npm install && npm run dev
```
