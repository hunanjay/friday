# 一、产品定位

一个依靠全渠道零感知输入，辅以极致 AI 自然语言检索的个人关系大脑。

# 二、目标受众

1. **To C (Prosumer)**：卖给销售、投资人、知识工作者，按月收订阅费（SaaS）。
2. **To B (Enterprise)**：包装成“高管 AI 领导力助手”，卖给中大型企业的 CEO/HRD。

# 三、核心业务闭环

### Step 1：冷启动破冰（存量初始化）
* **痛点**：传统 CRM 建库成本太高。
* **方案**：开源 MVP 版本直接基于 **Outlook (Microsoft Graph API)**。一键授权后，后台自动拉取历史邮件与contact，AI 瞬间解析生成详尽的联系人画像，支持粘贴微信聊天记录 文本或图片

### Step 2：零感知增量维护（日常录入）
* **痛点**：日常填写表单难以坚持。
* **方案**：
  * **全自动**：基于 Outlook 持续静默监听新邮件往来，提取最新动态。
  * **半自动**：Chat with AI（随口说、随手记），补充线下见面的非结构化信息。

### Step 3：极致输出（自然语言交互）
* **方案**：基于 RAG（检索增强生成）与 Text2SQL 技术，用户只需用自然语言提问（如“帮我找一个懂医疗且下周有空的北京投资人”），系统即可精准召回。

# 四、底层技术共识

### 1. 架构选型
采用 **关系型数据库**（推荐 **PostgreSQL + JSONB + pgvector**）。

### 2. 选型逻辑
* **产品形态匹配**：产品是以“用户本人”为中心的星型拓扑关系，关系链路浅，不需要图数据库的深度图遍历计算。
* **Agent 友好**：目前大模型的 Text2SQL（查关系型库）能力远比 Text2Cypher（查图库）成熟且稳定，更有利于自然语言查询的落地。

### 3. 数据接入与前端策略
* **数据源 (MVP)**：全面拥抱 **Outlook (Microsoft Graph API)**，接口极度成熟，且精准切中高价值商务人群。
* **前端策略**：只做 **响应式 Web (H5 + PC)**。开发成本低，方便跨平台使用。

---

# 五、MVP 落地执行补充说明 (Execution Details)

### 1. 核心信息架构 (Information Architecture)
MVP 版本 Web 端包含以下核心页面：
* **交互中枢 (Chat/Dashboard)**：核心输入框，支持长文本粘贴（用于初始化）和短文本/语音输入（用于日常随记），以及自然语言提问（搜索）。
* **联系人列表页 (Contact List)**：展示所有已建档的联系人，支持基础条件过滤。
* **联系人详情页 (Contact Profile)**：
  * **静态画像**：AI 提取的结构化标签（基础信息、商务信息、个人喜好等）。
  * **动态时间线 (Timeline)**：你与该联系人的历史交互记录（基于导入的聊天记录和日常随记生成）。

### 2. 核心数据实体定义 (Data Model)
AI 提取的 `Contact` (联系人) 画像至少需包含以下标准维度，作为 JSONB 存储：
* **【基础信息】**：姓名、公司、职位、所在地、联系方式。
* **【商务信息】**：对方核心痛点、合作历史、商业诉求、业务规模。
* **【私人信息】**：家庭状况、性格标签、个人喜好（忌口、运动等）、重要纪念日。
* **【动态更新】**：最近一次关键事件/约定。

### 3. 用户关键路径 (UX Flow - 冷启动)
* **粘贴动作**：用户在输入框粘贴长段微信聊天记录。
* **处理反馈**：鉴于大模型处理长文本需要时间，前端需设计“异步排队/处理中”的动画或进度条（如：“AI 正在深度阅读过去一年的故事…”），缓解等待焦虑。
* **结果确认**：AI 生成画像后，允许用户手动进行微调或纠错，确保首因效应（信任度）。

### 4. AI 处理流设计 (LLM Pipeline)
大模型在后端扮演双重角色：
* **信息抽取器 (Extractor)**：输入无格式聊天记录 -> 执行过滤Prompt (去掉“哈哈”等废话) -> 执行结构化Prompt -> 输出 JSON 格式画像。
* **意图与查询引擎 (Retriever)**：输入用户问题 -> 判断是 RAG 检索还是 Text2SQL -> 转化查询语句 -> 查询 Postgres -> 生成自然语言回答。

### 5. 开源演进与商业化路线 (GTM & Privacy)
* **第一阶段：开源 MVP (Outlook Base)**：利用成熟的 Outlook 生态验证“AI 关系大脑”逻辑，吸引极客和高势能种子用户。
* **第二阶段：商业化进阶 (自建 SMTP + 本地化)**：验证成功后，推出支持所有邮箱的**商业版（自研 SMTP/IMAP 引擎）**。
* **终极护城河（绝对隐私）**：主打**数据库 100% 本地化/私有化部署**，彻底消除大企业 CEO 和投资大佬对“人脉数据泄露”的顾虑。